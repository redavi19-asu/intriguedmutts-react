{
  "dependencies": {
    "autoprefixer": "^10.4.24",
    "framer-motion": "^12.33.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18"
  },
  "scripts": {
    "deploy": "cd intriguedmutts-react && npm run build && cd .. && npx gh-pages -d intriguedmutts-react/dist -b gh-pages",
    "deploy:full": "npm run deploy"
  }
}
